rules_version = '2';
// Emulador local: reglas permisivas (el emulador ignora auth; todo corre en 127.0.0.1).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
