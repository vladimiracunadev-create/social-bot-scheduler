# Mosquitto (Case 17) — broker MQTT local del laboratorio.
# Acceso anónimo: aceptable en el modelo local-only (todo bindeado a 127.0.0.1).
listener 1883
allow_anonymous true
persistence false
log_dest stdout
