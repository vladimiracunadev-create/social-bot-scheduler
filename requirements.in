# Top-level Python dependencies for the shared tooling / hub.
# Compile with:  pip-compile --generate-hashes requirements.in
# The generated requirements.txt pins exact versions with SHA256 hashes so
# `pip install -r requirements.txt` runs in --require-hashes mode (P-01).
python-dotenv
requests
pydantic>=2.0
pydantic-settings
pytest
pytest-cov
mypy
types-requests
PyYAML
werkzeug>=3.1.5
wheel==0.46.2
jaraco.context==6.1.0
